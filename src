module.exports = async function (context, req) {
context.log('API Serverless executada com sucesso!');


const name = req.query.name || (req.body && req.body.name);


context.res = {
status: 200,
body: {
message: "Azure Functions!",
input: name || "Teste"
}
};
}
```



